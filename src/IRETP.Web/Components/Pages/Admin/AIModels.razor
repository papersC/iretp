@page "/admin/ai-models"
@rendermode InteractiveServer

<PageTitle>IRETP — AI model status</PageTitle>

@* Thin route host — the actual UI lives in AIModelsPanel so the admin hub can
   embed the same view as a tab (see AdminHome). *@
<AIModelsPanel />
