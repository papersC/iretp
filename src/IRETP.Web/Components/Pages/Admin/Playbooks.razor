@page "/admin/playbooks"
@rendermode InteractiveServer

<PageTitle>IRETP — EWRS playbooks</PageTitle>

@* Thin route host — the actual UI lives in PlaybooksPanel so the admin hub can
   embed the same tool as a tab (see AdminHome). *@
<PlaybooksPanel />
