@page "/admin/developers/compare"
@rendermode InteractiveServer

<PageTitle>IRETP — Developer comparison</PageTitle>

@* Thin route host — the actual UI lives in DeveloperComparisonPanel so the admin
   hub can embed the same tool as a tab (see AdminHome). *@
<DeveloperComparisonPanel />
