@page "/admin/fabric"
@rendermode InteractiveServer

<PageTitle>IRETP — Microsoft Fabric / OneLake status</PageTitle>

@* Thin route host — the actual UI lives in FabricStatusPanel so the admin hub can
   embed the same view as a tab (see AdminHome). *@
<FabricStatusPanel />
